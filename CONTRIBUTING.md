## Setup

1.  Install packages

    ```sh
    npm install
    ```

1.  Set your timezone to `Europe/Warsaw`

    ```sh
    export TZ=Europe/Warsaw
    ```

1.  Run the tests

    ```sh
    make test-watch
    ```
